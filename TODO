New domains now work, however multiple clients don't work. A new client connects and kills the previous client.

Remove "Checking" since now we don't have to wait for the setInterval

Redirect on success (Same as isupChecker script)

Validate URL input (At the moment I add http:// on the server side. Plus we should strip out just the domain and use that.

Buy domain
